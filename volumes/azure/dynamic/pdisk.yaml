apiVersion:
kind:
metadata:
  name:
spec:
  replicas:
  selector:
    matchLabels:
      app:
  template:
    metadata:
      labels:
        app:
    spec:
      containers:
        - name:
          image:
          ports:
            - containerPort:
          volumeMounts:
            - name:
              mountPath:
      volumes:
        - name:
          persistentVolumeClaim:
            claimName:
---
# pvc
apiVersion:
kind:
metadata:
  name:
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName:
---
# storageClass
apiVersion:
kind:
metadata:
  name:
provisioner:
volumeBindingMode:
allowVolumeExpansion:
reclaimPolicy:
parameters:
  skuName: Standard_LRS
  cachingMode:
  kind:
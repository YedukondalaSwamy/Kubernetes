apiVersion:
kind:
metadata:
  name:
spec:
  replicas:
  selector:
    matchLabels:
      app:
  template:
    metadata:
      labels:
        app:
    spec:
      containers:
        - name:
          image:
          ports:
            - containerPort:
          volumeMounts:
            - name:
              mountPath:
      volumes:
        - name:
          persistentVolumeClaim:
            claimName:
---
# PVC
apiVersion:
kind:
metadata:
  name:
spec:
  accessModes:
    - ReadWriteMany
  resiurces:
    requests:
      storage: 5Gi
  storageClassName:
---
# storageclass
apiVersion:
kind:
metadata:
  name:
provisioner:
reclaimPolicy:
volumeBindingMode:
peramaters:
  skuName:

